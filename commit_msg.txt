Rewrite DETR and autoregressive decoders from scratch

Replace nn.TransformerDecoder with manual implementations:

- CrossAttention: Q from decoder, K/V from encoder memory
- DecoderLayer: Self-Attn → Cross-Attn → FFN (Pre-LN residuals)
- DETRDecoder: N learned queries through stacked DecoderLayers
- AutoregressiveDecoder: causal mask + sequential generation

Reuses MultiHeadAttention and FeedForwardNetwork from encoder.py.
Now all 4 model files (encoder + 3 decoders) are fully from-scratch
with zero high-level nn.Transformer* calls.
